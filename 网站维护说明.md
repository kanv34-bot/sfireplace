# sfireplace 网站维护说明

更新时间：2026-05-16

这份文档是给你下次自己修改网站用的。以后优先看这里，不要再找旧目录。

## 1. 正式版本在哪里

本地正式目录：

```text
/Users/x/Desktop/bizong-工作副本-20260514-224058
```

GitHub 正式仓库：

```text
https://github.com/kanv34-bot/sfireplace
```

GitHub 分支：

```text
main
```

Vercel 正式网站：

```text
https://sfireplace.com
```

Vercel 项目信息：

```text
项目名：sfireplace
项目 ID：prj_3hrjMCSneQUNnhKxwY5Yq06mSqhW
团队：kanvkanv3s-projects
```

现在本地正式目录和 GitHub `main` 是同一个版本。以后修改网站，只改本地正式目录这一份。

## 2. 当前网站技术

这是一个 Next.js 网站，不是 Astro。

主要命令：

```bash
npm run dev
npm run build
vercel --prod --yes
```

本地预览常用地址：

```text
http://127.0.0.1:3001/zh
```

如果要重新启动本地预览，可以在项目目录执行：

```bash
npm run dev -- -p 3001
```

## 3. GitHub 和 Vercel 的关系

GitHub 是代码正式版本，Vercel 是线上网站部署平台。

目前状态：

- GitHub `main` 已经是最新版。
- Vercel 线上网站已经手动部署到最新版。
- Vercel 还没有完成 GitHub 自动同步授权。

也就是说：现在网站是同步的，但以后你如果只上传 GitHub，Vercel 不一定会自动更新。

要让以后 GitHub 自动同步到 Vercel，需要在 Vercel 里做一次授权：

1. 登录 Vercel。
2. 进入账号设置。
3. 找到 Login Connections 或 Git Connections。
4. 连接 GitHub 账号。
5. 回到项目 `sfireplace`。
6. 连接 GitHub 仓库 `kanv34-bot/sfireplace`。
7. Production Branch 选择 `main`。
8. Framework 选择 `Next.js`。

如果还没有做这一步，修改后上线就用手动部署：

```bash
vercel --prod --yes
```

## 4. 网站文件布局

页面文件都在：

```text
src/app
```

多语言页面都在：

```text
src/app/[lang]
```

常用页面对应关系：

```text
首页：src/app/[lang]/page.tsx
产品列表页：src/app/[lang]/products/page.tsx
产品详情页：src/app/[lang]/products/[id]/page.tsx
案例列表页：src/app/[lang]/cases/page.tsx
案例详情页：src/app/[lang]/cases/[id]/page.tsx
新闻列表页：src/app/[lang]/news/page.tsx
新闻详情页：src/app/[lang]/news/[id]/page.tsx
视频页：src/app/[lang]/videos/page.tsx
关于我们：src/app/[lang]/about/page.tsx
联系我们：src/app/[lang]/contact/page.tsx
```

公共组件：

```text
顶部导航：src/components/Header.tsx
底部页脚：src/components/Footer.tsx
产品图片组件：src/components/ProductImage.tsx
```

全站样式：

```text
src/app/globals.css
```

网站基础 SEO：

```text
src/app/layout.tsx
```

## 5. 内容数据在哪里改

产品数据：

```text
src/lib/products.ts
```

案例文章：

```text
src/lib/cases.ts
```

新闻文章：

```text
src/lib/news.ts
```

语言配置：

```text
src/lib/dictionary.ts
```

普通页面文字翻译：

```text
src/messages/zh.json
src/messages/en.json
src/messages/de.json
src/messages/fr.json
src/messages/es.json
src/messages/pt.json
src/messages/ar.json
```

更细的字段翻译：

```text
src/lib/lang-data/en.ts
src/lib/lang-data/de.ts
src/lib/lang-data/fr.ts
src/lib/lang-data/es.ts
src/lib/lang-data/pt.ts
src/lib/lang-data/ar.ts
```

当前重点产品方向：

```text
电子壁炉
雾化壁炉
雾化壁炉定制
酒精壁炉
投影壁炉
```

以后写案例、新闻、SEO 和首页文案，都优先围绕这几类产品。

## 6. 图片和视频放在哪里

所有网站可访问的图片、视频都应该放在：

```text
public/media
```

网站里引用时不要写 `public`，要从 `/media/...` 开始。

例如文件在：

```text
public/media/news/2026-05-16.png
```

页面里引用应该写：

```text
/media/news/2026-05-16.png
```

不要写：

```text
public/media/news/2026-05-16.png
```

## 7. 图片目录说明

首页图片：

```text
public/media/home
```

首页产品分类图：

```text
public/media/home/category-illustrations
```

首页认证图标：

```text
public/media/home/certifications
```

首页项目案例图片：

```text
public/media/home/project-cases
```

首页大图、工厂图、空间图：

```text
public/media/home/section-illustrations
```

首页小图标：

```text
public/media/home/ui-icons
```

新闻文章插图：

```text
public/media/news
```

新闻图片现在按发布日期命名，例如：

```text
2026-05-16.png
2026-05-15.png
2026-05-14.png
```

新闻文章会根据 `src/lib/news.ts` 里的 `date` 自动找对应图片：

```text
/media/news/文章日期.png
```

原始素材备份：

```text
public/media/source
```

## 8. 产品、案例、视频素材目录

从旧外部 OSS 下载回来的本地素材在：

```text
public/media/oss
```

产品图片：

```text
public/media/oss/产品
```

其中重点产品相关目录：

```text
public/media/oss/产品/电子壁炉
public/media/oss/产品/雾化壁炉
public/media/oss/产品/定制壁炉
public/media/oss/产品/酒精壁炉
```

旧案例图片：

```text
public/media/oss/案例
```

视频文件：

```text
public/media/oss/视频
```

视频页代码在：

```text
src/app/[lang]/videos/page.tsx
```

视频页里的 `src` 都应该指向本地路径，例如：

```text
/media/oss/视频/瑞士Ruegg视频/Surprise.mp4
```

## 9. 常见修改怎么找

改首页第一屏：

```text
src/app/[lang]/page.tsx
public/media/home/section-illustrations
```

改首页项目案例图片：

```text
public/media/home/project-cases
src/app/[lang]/page.tsx
```

改案例文章：

```text
src/lib/cases.ts
```

改新闻文章：

```text
src/lib/news.ts
```

改新闻文章图片：

```text
public/media/news
```

改产品资料：

```text
src/lib/products.ts
```

改顶部菜单：

```text
src/components/Header.tsx
```

改底部联系方式：

```text
src/components/Footer.tsx
```

改 SEO 标题和描述：

```text
全站基础 SEO：src/app/layout.tsx
案例页 SEO：src/app/[lang]/cases/page.tsx
案例详情 SEO：src/app/[lang]/cases/[id]/page.tsx
新闻详情 SEO：src/app/[lang]/news/[id]/page.tsx
```

## 10. 修改后怎么检查

在本地正式目录执行：

```bash
npm run build
```

如果通过，再打开本地预览检查页面：

```text
http://127.0.0.1:3001/zh
```

重点检查：

```text
/zh
/zh/products
/zh/cases
/zh/cases/case_1
/zh/news
/zh/news/news_1
/zh/videos
/zh/contact
```

## 11. 修改后怎么上传 GitHub

在本地正式目录执行：

```bash
git status
git add -A
git commit -m "Update website content"
git push origin main
```

查看 GitHub：

```text
https://github.com/kanv34-bot/sfireplace
```

## 12. 修改后怎么发布到 Vercel

如果 Vercel 已经连接 GitHub，推送到 GitHub 后会自动部署。

如果还没有自动连接，就手动部署：

```bash
vercel --prod --yes
```

部署成功后检查正式站：

```text
https://sfireplace.com
```

## 13. 注意事项

不要再使用旧目录：

```text
/Users/x/Desktop/bizong
/Users/x/Desktop/sfireplace
```

这些旧目录已经删除。以后只认：

```text
/Users/x/Desktop/bizong-工作副本-20260514-224058
```

不要把图片和视频放在桌面临时文件夹里长期引用。网站上线要稳定，图片和视频必须放进：

```text
public/media
```

GitHub 单个文件硬限制是 100MB。现在最大视频约 57MB，可以上传，但 GitHub 会提示超过推荐的 50MB。如果以后视频很大，最好先压缩到 50MB 以下。

如果图片替换后页面没变化，优先检查三件事：

1. 图片是否放在 `public/media` 里面。
2. 页面引用路径是否从 `/media/...` 开始。
3. 是否重新部署了 Vercel。
