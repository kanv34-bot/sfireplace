# 价格同步说明

## 本地价格表

价格源表放在桌面：

`/Users/x/Desktop/sfireplace产品价格总表.csv`

以后只改这一列：

`人民币源价`

其他国家价格是网站按固定汇率自动换算出来的，不需要手工改。

## 同步命令

在网站工作副本目录运行：

```bash
npm run sync:prices
```

这个命令会读取桌面 CSV，并更新：

`src/lib/product-price-table.ts`

## 同步后的流程

1. 修改桌面价格表。
2. 运行 `npm run sync:prices`。
3. 运行 `npm run build` 检查。
4. 提交 GitHub。
5. 部署 Vercel。

## 线上检查页面

- `/zh/admin/price-matrix`
- `/zh/admin/products`
- `/ja/products/p3_15`

